explanation of what the project contains 

.env: Likely contains environment variables for the project, such as database connection strings, API keys, or configuration settings.

app.js: is the main entry point for the application, where the Express server is initialized.

examples.http: Possibly contains example HTTP requests for testing the API endpoints .

package.json: Defines the project's dependencies, scripts, and metadata.

package-lock.json: Provides a lock file for dependencies to ensure consistent installations.

config.js: contains configuration settings for the application, such as database or server configurations.

serverConnection.js: handles the server or database connection setup.

middleware/admin.js: Middleware for handling admin-specific logic, such as access control.

middleware/auth.js: Middleware for authentication, possibly validating tokens or sessions.

model/cards.js: A model for cards, likely defining the schema and methods for handling card-related data.

this is a project for my university not a real database that show my capabilities in node.js

connection string:"mongodb+srv://user:user@cluster0.vwbjx.mongodb.net/db?retryWrites=true&w=majority&appName=Cluster0"

atlas database
name:user
password:user

registred users

user:john.doerr@aleeex.com
password:securePassword1

user:admin@admin.com
password:securePassword1

user:not@business.com
password:securePassword1
